# Hidden Object Cross Correlation

A small hidden object game that takes an image file, randomly determines a smaller section of the image which has to be found. THe validation then happens via cross correlation from OpenCV and it gives a percentage how well the clicked position matches. UI made with Java Swing. 
